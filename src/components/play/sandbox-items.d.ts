/** Types for the vendored `sandbox-items.js`: the toy objects on the sandbox desk. */
export function buildSandboxItems(painters: Record<string, unknown>): unknown[];
export const SANDBOX_STOPS: { wide: number[][]; tall: number[][] };
export const SANDBOX_DESK_COLOR: string;
