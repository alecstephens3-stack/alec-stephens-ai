/** Types for the vendored `bench-items.js`: one object per build on Alec's bench. */
export function buildBenchItems(painters: Record<string, unknown>): unknown[];
export const BENCH_STOPS: { wide: number[][]; tall: number[][] };
export const BENCH_DESK_COLOR: string;
